# 推送到远程仓库

由于当前环境无法直接访问GitHub，请在本机终端执行以下命令：

```bash
cd /path/to/Ginkoo

git remote add origin https://github.com/wzhang-code/Ginkoo.git
git push -u origin 260310-feat-create-ginkoo-website
```

或者如果已添加过origin，直接执行：
```bash
git push -u origin 260310-feat-create-ginkoo-website
```
